# 学习日志

## 2026-03-05：项目初始化

- 完成了本地 Git 仓库初始化，并关联到 GitHub 远程仓库。
- 理解了 Git 负责记录项目历史，GitHub 是远程备份与协作平台。
- 建立了 src、tests、workspace/notes 等目录。
- 当前疑问：Python 虚拟环境是什么，为什么项目需要它？

## 2026-03-05：Python 开发环境

- 用 Python 3.12.13 创建了项目专属的 .venv 虚拟环境。
- 激活后，python 指向项目目录中的 .venv/bin/python。
- 退出后，python3 回到系统全局的 Python 3.14。
- 安装并验证了 pytest 9.1.1。
- 理解了虚拟环境用于隔离不同项目的依赖。

## 2026-03-05：安全配置加载

- 安装了 python-dotenv，用于从本机 .env 文件读取配置。
- 创建了公开的 .env.example 和不会提交的本机 .env。
- 实现了模型供应商、模型名称与 API Key 的配置校验。
- 验证了 API Key 不会被打印到终端。
- 验证了缺失配置时会显示明确错误。

## 2026-03-05：第一个终端聊天程序

- 使用 DeepSeek 的 OpenAI 兼容接口完成了单轮模型调用。
- 理解了终端输入、配置加载、模型请求和终端输出之间的数据流。
- 设置了 max_tokens=512，用于限制首版回答长度与成本。
- 为无效 API Key、网络连接、限流和服务端错误添加了友好提示。
- 验证了空消息不会调用 API。
- 验证了临时覆盖无效 API Key 后，程序会显示明确错误。

## 2026-03-05：多轮上下文

- 将模型调用从单条用户消息改为接收完整消息历史。
- 在 main.py 中维护了本次运行期间的 conversation 列表。
- 理解了模型 API 是无状态的，多轮记忆来自每次重新发送的历史消息。
- 在模型调用失败时移除本轮用户消息，避免破坏角色顺序。
- 使用虚构昵称完成了连续五轮上下文验证。
- 确认当前记忆只存在于内存，程序关闭后会清空。

## 2026-03-05：人格与行为边界

- 创建了 workspace/SOUL.md，用 Markdown 定义 Agent 的身份、沟通方式与行为边界。
- 创建了 soul.py，在程序启动时读取并验证人格文件。
- 将 system 消息从 main.py 移到 SOUL.md。
- 验证了修改称呼后，重启程序立即使用新设定。
- 验证了 SOUL.md 缺失时，程序会停止启动并显示明确提示。
- 理解了 SOUL.md 是行为指引，不是安全权限控制。

## 2026-03-05：错误处理与基础日志

- 新增了 LLM_TIMEOUT_SECONDS 配置，用于控制模型请求超时。
- 创建了 logging_config.py，将运行日志写入本地 logs/app.log。
- 日志只记录事件类型、消息数量、状态码和超时阈值，不记录 API Key、用户输入或模型回答。
- 验证了正常聊天后会生成日志。
- 验证了虚构敏感标记不会写入日志。
- 验证了模型超时时，终端显示简洁提示，日志保留超时原因。
- 验证完成后将超时配置恢复为 30 秒。

## 2026-03-05：受控工具调用循环

- 创建了 tools.py，将工具说明、工具白名单和执行逻辑分开管理。
- 添加了无副作用的 get_current_time 工具。
- 实现了模型请求工具、程序执行工具、工具结果回传模型的完整循环。
- 理解了模型只能请求工具，真正执行权限由 Python 代码决定。
- 添加了未知工具、额外参数、非法参数和工具异常的受控处理。
- 设置了单轮最多执行 3 次工具的安全上限。
- 验证了时间工具调用成功，以及上限为 0 时会被安全阻止。

## 2026-03-06：安全计算工具

- 在 tools.py 中新增了计算工具，负责确定性算术计算。
- 使用 Python 的 ast 将表达式解析成语法树，不使用 eval() 执行输入内容。
- 只允许数字、括号、指定的算术运算符，以及受限的幂运算。
- 为表达式长度、数值范围和幂指数设置了限制，避免异常资源消耗。
- 验证了一个算术表达式的结果为 79.4，且 Agent 会调用该工具。
- 验证了代码形态的函数调用表达式会被拒绝，未被执行。

## 2026-03-06：受限笔记文件工具

- 新增了受限笔记工具，只能在 workspace/notes 目录中创建和读取笔记。
- 使用实际路径检查，拒绝 ../、绝对路径和符号链接造成的目录越界。
- 限制了笔记文件名、内容长度和文件类型，只接受单个 .md 文件名。
- 使用新建文件模式，已有同名笔记一律拒绝覆盖。
- 理解了模型传入的参数不等于用户授权，不能把模型给出的覆盖标记当作确认。
- 验证了创建和读取笔记、拒绝越界路径、拒绝同名覆盖，以及 Agent 写入后读取笔记的完整流程。

## 2026-03-06：保存和恢复会话

- 新增了 session_store.py，使用本机 JSONL 文件保存已完成的会话消息。
- 每次启动都从当前 SOUL.md 读取人格设置，不保存旧的 system 消息。
- 理解了 JSONL 以一行一条记录的方式追加保存，不会覆盖已有会话。
- 理解了模型或工具失败时应先回滚本轮消息，避免保存不完整的会话记录。
- 验证了重启后能恢复历史消息，并找回前一次会话中的虚构信息。
- 验证了遇到一条损坏 JSON 时会跳过该行，同时保留其他有效记录。

## 2026-03-06：上下文压缩

- 新增了上下文管理模块，用字符数估算判断是否需要压缩历史。
- 理解了不能按单条消息截断，因为一次工具调用可能包含多条必须连续保留的消息。
- 保留了最近一个完整用户回合，将更早的完整回合交给模型生成摘要。
- 将摘要作为普通历史上下文加入，不把用户历史内容提升为 system 指令。
- 使用临时文件替换会话快照，保存摘要和最近消息，避免半截覆盖。
- 验证了压缩后仍能找回虚构事实和未完成任务，并在重启后加载历史摘要。

## 2026-03-06：长期记忆

- 新增了本机 Markdown 长期记忆文件，并将其加入 Git 忽略规则。
- 使用 save_memory 和 search_memory 工具保存与检索长期记忆。
- 使用 /remember 内容作为明确授权入口，不把普通聊天自动保存为长期记忆。
- 校验了模型请求的保存内容必须与本轮授权内容一致。
- 验证了未授权保存会被拒绝，明确授权后可保存并检索。
- 验证了检索不到内容时，工具会如实返回没有匹配结果。

## 2026-03-06：受控天气查询工具

- 使用 Open-Meteo 的公开接口实现了城市天气查询，不需要额外 API Key。
- 先通过地理编码将城市名转换为经纬度，再查询当前模型天气。
- 设置了网络超时、状态码和返回数据结构检查，不向用户暴露内部异常。
- 将天气工具设为只读工具，模型只能查询，不能改变外部数据。
- 关闭了 httpx 的信息级请求日志，避免城市名出现在本地日志中。
- 验证了正常查询、找不到城市、网络连接失败和 Agent 工具调用流程。

## 2026-03-06：每日复盘技能

- 为每日复盘定义了固定的输出结构和安全边界。
- 添加了只读的技能加载能力，模型可以按需读取任务工作流。
- 限制了技能名称、读取目录、文件大小和内容格式，避免读取任意本地文件。
- 理解了任务技能是提供给模型的工作流说明，不是自动执行的代码。
- 验证了指定技能能被加载，缺失技能时会返回明确错误。
- 验证了 Agent 会先读取技能，再按模板输出复盘，普通聊天不受影响。
- 明确了未要求保存时只能在聊天中输出，复测中未调用笔记写入工具。

## 2026-03-06：渠道适配层

- 定义了统一的入站消息、出站消息和消息渠道接口。
- 将终端输入输出转换为统一的消息格式，不再让 Agent 核心直接依赖终端。
- 理解了渠道负责收发消息，Agent 核心负责会话、记忆、工具和模型调用。
- 为消息补充了渠道、会话和发送者标识，为后续接入真实 Bot 做准备。
- 验证了重构后正常聊天、空消息拒绝、退出和重启恢复会话仍然可用。
- 验证了非文本内容不能作为合法的文本消息进入 Agent 核心。

## 2026-03-06：Telegram Bot 渠道

- 使用 Telegram 的长轮询方式接收消息，并将 Agent 回复发送回原私聊。
- 将 Agent 的会话、记忆、工具和模型逻辑从终端启动流程中进一步抽离，供 Telegram 复用。
- 使用本机配置保存 Bot Token 和唯一允许的用户 ID，不将这些信息写入代码或 Git。
- 限制只接受白名单用户的私聊文字消息，其他用户和群聊消息不会进入 Agent。
- 验证了手机文字对话、/quit 不停止服务、图片消息受控提示和本机 Ctrl+C 停止。
- 验证了构造的非白名单用户和群聊条件会被拒绝；尚未使用第二个真实账号进行此项验证。

## 2026-03-06：定时提醒任务

- 添加了受控的短时间提醒命令，用于在指定秒数后向 Telegram 私聊发送提醒。
- 限制了提醒等待时间、提醒内容长度和同时运行的任务数量，避免无限制创建后台任务。
- 理解了定时提醒不需要调用模型，固定任务应直接由程序执行。
- 将提醒命令和提醒发送与主聊天会话隔离，不保存到聊天历史。
- 验证了短时间提醒会按预期送达，无效秒数会被拒绝，普通聊天仍不包含提醒内容。
- 当前未触发的提醒会在程序停止时取消，不会跨重启恢复。

## 2026-03-07：自动化测试与安全回归

- 使用 pytest 将关键安全规则变成可重复执行的自动化测试。
- 覆盖了 Telegram 白名单配置、受限笔记路径、安全计算、会话保存与损坏记录恢复、定时提醒命令。
- 测试使用临时目录和临时环境变量，不读取真实会话、笔记或私密配置。
- 验证了 12 项测试全部通过。
- 临时修改白名单判断后，相关测试立即失败；恢复判断后全部测试重新通过。

## 2026-03-07：可复现交付与最终验收

- 补充了固定版本的直接依赖清单、公开安装说明、配置说明、安全边界、限制和常见问题。
- 使用独立的 Python 3.12 虚拟环境重新安装依赖；依赖检查无错误，12 项测试全部通过。
- 验证了终端中工具调用后的会话保存与重启恢复；重新启动后能从历史中找回前一轮的计算结果。
- 验证了 Telegram 私聊文字回复和短时提醒均能正常送达。
- 明确区分了会话记忆与长期记忆：普通“请记住”不会产生长期保存，只有 `/remember` 才构成明确授权。
- 检查了待提交文件和 Git 忽略规则，未发现私密运行数据被版本控制跟踪。

## 2026-03-07：Gateway 架构边界

- 明确第二阶段采用“本机 Gateway + CLI 与渠道客户端”的结构，而不是直接复刻官方全部能力。
- 固定了调用方向：Gateway 统一协调 Agent、会话、工具和任务；CLI 与 Telegram 只能请求 Gateway。
- 为会话定义稳定的 `session_id`，避免不同入口未来共享或混淆历史记录。
- 规定 Gateway 默认只监听本机，并在处理聊天和工具请求前校验本机 Token。
- 明确旧会话和长期记忆迁移前必须备份，不能由新服务静默覆盖。
- 验证了架构文档格式正常，现有 12 项自动化测试仍全部通过。

## 2026-03-07：可安装包与 CLI 骨架

- 将项目整理为可安装的 Python 包，并提供 `myclaw` 命令作为后续 Gateway 运维入口。
- 当前命令支持帮助、版本和安全配置检查，不会直接创建 Agent、读取会话或调用模型。
- 使用打包元数据声明运行依赖，并将测试依赖标记为开发环境可选依赖。
- 为 CLI 补充了安全配置失败和未知命令拒绝测试。
- 在独立 Python 3.12 环境中重新安装后，命令可用、配置摘要隐藏 Key、依赖检查无错误，15 项测试全部通过。

## 2026-03-07：SQLite 会话状态与安全迁移

- 建立了按 `session_id` 隔离的 SQLite 状态模型，可保存会话消息、历史摘要和后续持久任务所需的基础字段。
- 使用事务替换会话快照，确保摘要与保留消息不会出现半截写入。
- 限制会话标识的字符和长度，拒绝路径形式或不合法的标识进入状态层。
- 迁移旧会话时先创建按内容哈希命名的备份，再从备份副本导入；原始记录不会被删除或覆盖。
- 目标会话已存在时拒绝迁移，避免重复导入；损坏记录会跳过并保留有效记录。
- 暂不迁移真实聊天记录，因为旧终端运行时仍在写入原记录；等待 Gateway 接管写入后再执行一次迁移，避免两份记录分叉。
- 验证了会话隔离、快照替换、非法会话标识、损坏记录恢复、备份复用和重复迁移拒绝；全部自动化测试增加至 22 项并通过。

## 2026-03-07：仅本机 Gateway 生命周期

- 建立了只监听 `127.0.0.1` 的常驻 Gateway，当前仅提供健康检查和运行状态，不处理聊天。
- 发现原阶段顺序与“所有 Gateway 接口先校验 Token”的安全规则冲突，因此将最小 Token 校验提前到本步骤；后续消息接口将复用该认证边界。
- CLI 通过本机 HTTP 请求启动和查询 Gateway，不直接创建 Agent、读取会话或调用模型。
- 健康检查、状态查询、未知路径和写请求均在认证边界内处理；状态输出不会回显 Token。
- 使用脱敏日志记录 Gateway 的启动、停止和请求路径，不记录请求头、Token 或聊天内容。
- 验证了带 Token 的状态查询、无 Token 的拒绝、非本机绑定拒绝、停止后不可用提示和生命周期日志；全部自动化测试增加至 31 项并通过。

## 2026-03-07：Gateway 会话消息 API

- 建立了受 Token 保护的会话消息接口，只接受受限大小的 JSON 文本请求，并要求路径携带合法的 `session_id`。
- Gateway 成为调用 Agent、保存会话和触发历史压缩的唯一入口；CLI 只发送本机 HTTP 请求并展示结果。
- 模型或状态读取失败时不会留下半截用户消息；工具调用在 Gateway 中只记录工具名称，不记录参数或结果。
- 空消息、额外字段、非 JSON 请求、过长请求、非法会话标识和未认证请求都会在调用 Agent 前被拒绝。
- 旧记录迁移首次因默认会话已有 Gateway 验证记录而被安全拒绝；随后显式导入独立会话，保留旧记录与新记录，不自动合并。
- 验证了真实消息、重启后的会话恢复、非法会话标识拒绝和日志不记录测试消息正文；全部自动化测试增加至 42 项并通过。

## 2026-03-07：Gateway 运维 CLI

- 将会话列表和日志查看统一为 CLI 通过已认证 Gateway 发起的只读请求，CLI 不直接读取 SQLite 或本地日志。
- 会话列表只展示会话标识、消息数量、摘要状态和更新时间，不返回任何历史消息正文。
- 日志查看限制单次事件数量与读取字节数，只保留 Gateway 允许展示的事件类别。
- 请求日志改为记录稳定路由类别，不再记录原始会话路径；日志接口也会过滤旧日志中的动态字段。
- 验证了无 Token 的会话访问被拒绝、日志数量超过上限被拒绝，以及 Gateway 停止后不会由 CLI 创建第二个 Agent。
- Gateway 接口、客户端和 CLI 的自动化回归测试全部通过，测试总数增加至 50 项。

## 2026-03-07：Telegram Gateway 渠道迁移

- 将 Telegram 限定为接收文字、执行白名单校验、映射稳定会话标识和发送回复的渠道适配器。
- 普通 Telegram 文本只通过本机 Gateway 请求处理，不再直接创建 Agent、读取旧会话或调用模型与工具。
- Gateway 不可用时返回固定的安全提示，不回显 Token、内部错误、聊天内容或会话标识。
- 保留私聊白名单、非文本消息和群聊拒绝规则；Telegram 的退出指令仍不能停止服务。
- 旧的进程内 `/remind` 路径暂时停止使用，等待下一步改造成可跨重启恢复的 Gateway 持久化任务。
- 验证了真实私聊消息经 Gateway 得到回复，CLI 可在同一 Telegram 会话中恢复该轮历史，并确认只写入 Gateway 会话元数据；全部自动化测试增加至 58 项并通过。

## 2026-03-07：持久化 Gateway 提醒任务

- 将提醒统一为 Gateway 创建、SQLite 保存、后台扫描和 Telegram 投递的任务流程，CLI 与 Telegram 渠道不再自行维护定时器。
- 使用 `pending`、`delivering`、`delivered` 和 `failed` 状态记录提醒生命周期；未到期任务可在 Gateway 正常重启后继续等待。
- 任务列表只展示类型、状态和时间等元数据，不显示提醒正文或 Telegram 投递地址；提醒事件日志也只保留脱敏类别。
- CLI 与 Telegram 的 `/remind` 都只请求 Gateway 创建任务，普通聊天路径不承担提醒调度。
- 发送前先将任务标记为 `delivering`；发送失败会终结为 `failed`，不会自动重试，避免外部消息重复发送。
- 验证了短时提醒从创建状态转为已送达，任务查询和脱敏日志均可查看结果；全部自动化测试增加至 71 项并通过。

## 2026-03-07：Gateway 安全、运维与最终验收

- 为 Gateway 增加代码级工具白名单，模型只能看到被允许的工具；即使模型伪造越权调用，执行阶段仍会再次拒绝。
- Gateway 当前不向模型开放本地笔记写入工具；长期记忆仍须通过 `/remember` 的明确内容授权。
- 状态查询增加 Agent 运行器、状态存储、提醒服务和工具策略的固定诊断字段，不返回配置、Token、会话正文或任务正文。
- 补充了越权工具、固定诊断、损坏提醒任务和服务不可用等回归测试。
- 使用独立 Python 3.12 环境重新安装项目，依赖检查无错误，76 项自动化测试全部通过。
- 验证了 Gateway 启动后全部固定诊断正常、任务元数据可查询，并在验证后正常停止服务。
