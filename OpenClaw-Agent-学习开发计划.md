# 从零开发个人 Agent：学习与实施计划

## 0. 计划的定位

### 最终目标

开发一个单用户、单消息渠道的个人 Agent 系统（项目暂定名：`myclaw`）。它调用现成的大模型，具备：

- 多轮对话与人格设定；
- 会话持久化、上下文压缩与长期记忆；
- 安全受限的工具调用；
- 一个真实消息渠道（默认 Telegram，可替换）；
- 定时任务、基础并发控制、日志和测试。

这不是复刻官方 OpenClaw，也不包含多平台、插件生态、复杂 UI、生产级沙箱或多租户能力。我们要学习的是构建 Agent 的核心原理，并完成一个自己可实际使用的版本。

### 技术选择

- 语言：Python 3.12（当前环境已安装 3.12.13）；
- 模型：DeepSeek API；首个聊天模型使用 `deepseek-v4-flash`；
- 数据：本地 JSONL 与 Markdown 文件；
- 渠道：先终端，后接入 Telegram（若不可用，再替换为飞书、Discord 或 Slack）；
- 版本控制：Git。

选择 Python 的原因是语法较易入门，适合先理解 Agent 的运行机制；选择本地文件而非数据库，是为了先看清数据如何流动。

### 范围边界与安全原则

- 不训练模型；项目通过 API 调用现成模型；
- 不把模型生成的文本直接作为系统命令执行；
- 文件工具只能访问项目指定的工作目录；
- 写文件、外发消息等有副作用的操作，必须明确确认或严格限制；
- API Key、Bot Token、个人记忆和日志不得提交到 Git；
- 初版只服务一个经过授权的用户。

## 1. 我们如何协作

### 每次编码的固定流程

1. 先阅读本计划中当前步骤的目标、前置条件和验收标准。
2. 我解释：现有程序缺什么、这一小步解决什么、涉及哪些基础概念。
3. 我给出你要创建或修改的文件、推荐结构、完整示例和逐段说明。
4. **所有项目代码由你手动输入或粘贴并保存；我不直接替你修改源码。**
5. 你执行本步骤的验证，并把完整输出或完整错误发给我。
6. 我根据结果帮助你定位问题；修复仍由你自己完成。
7. 成功用例和失败用例都通过后，在本文件勾选该步骤，再继续下一步。
8. 每完成一个步骤，同步更新根目录本机保存的 `实际指导过程复盘.md`，记录实际操作、验证与可复用经验；该文档不提交到 Git。

### 代码教学注释约定

- 每次给出源码前，先说明数据如何流动、每个文件负责什么；
- 示例代码为关键概念、易错语法、设计取舍和安全边界添加中文注释；
- 不为 `import`、赋值等显而易见的语句机械添加逐行注释，避免注释遮蔽真正需要理解的内容；
- 你手动输入代码时也输入这些注释；遇到不理解的注释，先解释清楚再继续。

### 记录与提交约定

- 学习日志使用省略主语的客观记录句式，不使用“我”；
- 从本约定生效起，Git 提交信息使用中文；
- 已存在的历史提交保留原样，不为统一文案而改写已推送的历史。

### 计划调整规则

计划是路线图，不是必须死守的脚本。出现以下情况时，必须先调整计划再继续：

- 前置知识或环境与预期不符；
- API、SDK、消息平台的能力或价格发生变化；
- 某项实现带来不合理的安全风险；
- 验证暴露出架构问题；
- 你的真实需求变化。

调整时，我们会在本文件的“变更记录”补充日期、原因、改动和影响范围；不会悄悄跳过未完成的验收。

### 每一步的完成定义

每个步骤必须同时满足：

- 你能用自己的话解释该步骤解决的问题；
- 一个成功验证通过；
- 一个预期失败或边界验证通过；
- 不破坏前面已通过的关键功能；
- 没有新增敏感信息进入版本控制。

## 2. 进度总览

| 阶段 | 步骤 | 交付物 | 状态 |
| --- | --- | --- | --- |
| 准备 | 0.1–0.3 | 干净项目、Python 环境、安全配置 | 已完成 |
| 核心对话 | 1.1–1.4 | 终端聊天、上下文、人格、错误处理 | 已完成 |
| Agent 工具 | 2.1–2.3 | 工具循环、计算与时间工具、安全文件工具 | 已完成 |
| 记忆 | 3.1–3.3 | 会话持久化、压缩、长期记忆 | 已完成 |
| 外部能力 | 4.1–4.2 | 查询工具、Skills | 已完成 |
| 消息渠道 | 5.1–5.2 | 渠道适配层、一个真实 Bot 渠道 | 已完成 |
| 主动与可靠性 | 6.1–6.3 | 定时任务、队列、日志与测试 | 已完成（6.2 当前不适用） |
| 交付 | 7.1 | README、完整验收、复盘 | 已完成 |
| Gateway 与 CLI（第二阶段） | 8.1–8.9 | 本机 Gateway、会话隔离、CLI、渠道迁移与持久任务 | 未开始 |

## 3. 详细步骤

## 阶段 0：环境与项目基础

### 0.1 创建项目骨架与学习日志

**目标**：建立可持续维护的项目结构，理解终端、目录、文件与 Git 的最小用法。

**你将手写**：

- `README.md`：项目目的与启动说明；
- `.gitignore`：忽略虚拟环境、密钥、会话、日志；
- `learning-log.md`：记录今天学了什么、卡在哪里、自己如何理解；
- 初始目录：`src/`、`workspace/`、`tests/`。

**我会重点解释**：项目目录为什么要分层；Git 为什么不等于“云盘”。

**验证**：

- 能在项目根目录列出这些文件；
- Git 状态中不出现不应追踪的临时文件。

**边界验证**：在根目录故意创建 `.env`，确认 `.gitignore` 使它不会被 Git 追踪。

**完成状态**：- [x]

### 0.2 安装并验证 Python 开发环境

**目标**：拥有隔离、可复现的运行环境。

**你将手写或执行**：创建 Python 虚拟环境，安装最小依赖，并记录 Python 版本。

**我会重点解释**：Python、包管理器、虚拟环境和“依赖版本”的区别。

**验证**：运行一个最小 Python 文件，输出当前解释器版本和项目路径。

**边界验证**：退出虚拟环境后，确认项目依赖不会被系统 Python 意外使用。

**完成状态**：- [x]

### 0.3 创建安全配置加载器

**目标**：能读取模型配置，但不泄露任何 API Key。

**你将手写**：

- `.env.example`：只含变量名和无效示例；
- `src/config.py`：读取环境变量、检查必要配置；
- 本机 `.env`：只留在本机，不提交。

**我会重点解释**：环境变量、配置校验、为什么日志中也不能打印密钥。

**验证**：程序能显示“已配置/未配置”，但绝不显示完整 Key。

**边界验证**：删除 Key 后，程序给出明确的修复提示而不是堆栈崩溃。

**完成状态**：- [x]

## 阶段 1：最小对话程序

### 1.1 调用模型并实现终端聊天循环

**目标**：从终端读取一句话，调用模型 API，输出回答。

**你将手写**：

- `src/llm_client.py`：唯一负责调用模型的模块；
- `src/main.py`：输入、输出和 `/quit` 退出。

**我会重点解释**：模块职责、函数参数与返回值、网络请求可能失败的原因。

**验证**：输入一个简单问题，得到模型回复；输入 `/quit` 后正常退出。

**边界验证**：网络断开或 Key 错误时，给出友好错误信息。

**完成状态**：- [x]

### 1.2 加入多轮上下文

**目标**：让程序在一次运行期间记住前面的对话。

**你将手写**：消息列表以及将 `system`、`user`、`assistant` 消息传给模型的逻辑。

**我会重点解释**：模型本身不会自动记忆；“聊天记录”和“模型上下文”之间的关系。

**验证**：先告诉 Agent 一个昵称，再问它刚才听到的昵称。

**边界验证**：连续进行 5 轮对话，检查角色顺序和回答仍正确。

**完成状态**：- [x]

### 1.3 使用 SOUL.md 管理人格和边界

**目标**：把 Agent 的身份、语气和限制从代码中拆出来。

**你将手写**：`workspace/SOUL.md` 和读取它的函数。

**我会重点解释**：系统提示词的作用与局限；为什么提示词不是可靠的安全控制。

**验证**：修改称呼或回答风格，重启后立即生效。

**边界验证**：移走 `SOUL.md`，程序应提示缺少配置，而非静默使用未知行为。

**完成状态**：- [x]

### 1.4 统一错误处理与基础日志

**目标**：把可预期错误变成可读提示，并保留排错线索。

**你将手写**：基础日志配置、模型调用异常处理。

**验证**：正常聊天时生成不包含私密内容的运行日志。

**边界验证**：模拟 API 超时，用户收到简洁提示，日志保留技术原因。

**完成状态**：- [x]

## 阶段 2：Agent 与工具

### 2.1 实现工具调用循环

**目标**：模型能选择工具，程序执行并把工具结果发回模型，直到得到最终回答。

**你将手写**：工具声明、工具注册表、执行器、最大工具调用次数。

**我会重点解释**：工具参数模式、结构化输入、为什么要限制循环次数。

**验证**：使用无副作用的 `get_current_time` 工具回答“现在几点”。

**边界验证**：模型请求不存在的工具或超过次数上限，程序安全停止并提示。

**完成状态**：- [x]

### 2.2 加入可测试的计算工具

**目标**：理解“模型负责决策，程序负责确定性计算”的分工。

**你将手写**：只支持算术表达式、且不执行任意 Python 代码的 `calculate` 工具。

**验证**：询问一个算术问题，Agent 调用工具并给出结果。

**边界验证**：输入试图执行代码的表达式，工具明确拒绝。

**完成状态**：- [x]

### 2.3 加入受限笔记文件工具

**目标**：让 Agent 能读写笔记，同时不让它拥有整台电脑的文件权限。

**你将手写**：`read_note`、`write_note` 和工作目录路径校验。

**验证**：Agent 能在 `workspace/notes/` 新建并读取一份笔记。

**边界验证**：请求读取工作目录外文件或使用 `../` 路径时，必须被拒绝；首版一律拒绝覆写已有笔记。真正的交互式确认需要可信的会话状态，后续再单独实现，不能把模型传入的 `overwrite=True` 当作用户确认。

**完成状态**：- [x]

## 阶段 3：短期与长期记忆

### 3.1 持久化会话

**目标**：重启程序后保持对话记录。

**你将手写**：按会话保存和加载 JSONL 的模块。

**我会重点解释**：追加式存储、序列化、损坏记录的处理。

**验证**：对话后退出重启，Agent 能回答上一轮明示的信息。

**边界验证**：故意在文件中放入一行损坏 JSON，程序有明确处理策略且不丢掉全部记录。

**完成状态**：- [x]

### 3.2 实现上下文压缩

**目标**：防止对话越长，模型调用越贵、越慢或超过上下文限制。

**你将手写**：消息长度估算、摘要触发条件和摘要保存逻辑。

**验证**：用测试阈值触发一次摘要，最近对话仍保留。

**边界验证**：摘要前后检查关键事实和未完成任务没有消失。

**完成状态**：- [x]

### 3.3 实现长期记忆

**目标**：区分会话内容与明确要长期记住的信息。

**你将手写**：`save_memory`、`search_memory` 工具及 Markdown 记忆格式。

**验证**：使用明确的 `/remember 内容` 授权保存偏好，清空会话后仍可找回。

**边界验证**：普通闲聊不会被自动永久保存；没有 `/remember` 授权时，即使模型请求保存也会被代码拒绝；搜索不到时 Agent 如实说明。

**完成状态**：- [x]

## 阶段 4：外部信息与技能

### 4.1 加入一个受控网络查询工具

**目标**：让 Agent 查询实时信息，而不是假装知道。

**你将手写**：一个天气或网页查询工具，包含超时和失败处理。

**验证**：对实时问题，Agent 调用工具并说明信息来源或查询时间。

**边界验证**：断网或服务失败时，明确告诉用户查询失败，不能编造结果。

**完成状态**：- [x]

### 4.2 用 SKILL.md 添加任务技能

**目标**：把某类任务的工作流放进文档，而不是一直膨胀主程序。

**你将手写**：一个技能，例如“每日学习复盘”，以及技能加载逻辑。

**验证**：请求复盘时，Agent 读取并按技能模板输出。

**边界验证**：技能文件缺失或内容错误时，不影响普通聊天；未明确要求保存时，技能工作流只在聊天中输出，不应调用写入工具。

**完成状态**：- [x]

## 阶段 5：接入消息渠道

### 5.1 创建渠道适配层

**目标**：使终端、Telegram 等输入渠道可替换，而不用重写 Agent 核心。

**你将手写**：统一的入站消息结构和出站回复接口。

**验证**：重构后终端聊天仍通过阶段 1 的核心用例。

**边界验证**：空内容有明确处理；非文本内容不能构造为合法的文本入站消息。真实渠道中的图片、语音等消息处理留到 5.2 验证。

**完成状态**：- [x]

### 5.2 接入一个真实 Bot 渠道

**目标**：通过手机或常用聊天工具与 Agent 对话。

**默认选择**：Telegram。若无法使用，在本步骤开始前改用飞书、Discord 或 Slack，保持适配层不变。

**你将手写**：渠道适配器、Token 读取、自己的用户 ID 白名单。

**验证**：你从手机发送一句话，收到 Agent 回复。

**边界验证**：非白名单用户不会得到执行工具或访问记忆的机会；图片、语音等非文本消息有明确处理；Token 不出现在 Git 状态中。

**完成状态**：- [x]

## 阶段 6：主动工作与可靠性

### 6.1 实现定时任务

**目标**：Agent 能在指定时间主动执行受控任务。

**你将手写**：定时任务配置、独立任务执行器和一个“短时间后提醒”的测试任务。首版提醒不调用模型，也不写入主会话。

**验证**：测试任务在预期时间向你的渠道发送提醒。

**边界验证**：任务失败时记录日志；任务对话不污染主聊天会话。

**完成状态**：- [x]

### 6.2 多会话并发控制（当前范围不适用）

**原目标**：避免同一会话的并发消息造成记录错乱。

**调整原因**：当前项目明确只服务单用户，Telegram 长轮询在同一主循环中顺序处理消息；提醒任务也不读取或写入主会话。因此目前没有并发 Agent 回合，现有会话存储也不具备多会话隔离能力。

**未来触发条件**：接入多个授权用户、并行处理更新，或将耗时任务放入工作线程/进程后，再实现按会话键隔离的锁或队列，并同时改造会话存储。

**当前结论**：不为尚不存在的并发路径增加线程与锁，避免制造新的死锁、状态共享和调试复杂度。

**状态**：当前范围不适用；未来扩展时重新启用。

### 6.3 写自动化测试与安全回归检查

**目标**：让关键规则可重复验证，避免新功能破坏旧安全边界。

**你将手写**：至少覆盖配置校验、路径限制、会话读写、用户白名单的测试。

**验证**：一次运行通过全部测试。

**边界验证**：故意修改路径校验或白名单逻辑，测试应该失败。

**完成状态**：- [x]

## 阶段 7：交付与复盘

### 7.1 完成可复现交付

**目标**：未来的你能从 README 重建和使用该项目。

**你将手写**：完整 README、配置说明、威胁与限制说明、常见问题、最终学习复盘。

**最终验收流程**：

1. 在不使用现成运行数据的情况下配置环境；
2. 完成一次终端消息 → 工具调用 → 会话保存 → 重启恢复；
3. 通过真实消息渠道完成同样流程；
4. 验证非白名单用户、越界文件路径、错误配置都被正确拒绝或处理；
5. 运行自动化测试；
6. 检查 Git 状态，确认没有密钥、会话、日志或私人记忆被追踪。

**完成状态**：- [x]

## 阶段 8：Gateway 与 CLI（第二阶段）

### 第二阶段目标

将已完成的单用户 Agent 改造为“一个本机常驻 Gateway + 多个客户端入口”的结构：CLI 和 Telegram 都只与 Gateway 通信，由 Gateway 统一拥有 Agent、会话、工具、任务与状态。

本阶段使用项目自己的命令名 `myclaw`，不占用或伪装为官方 `openclaw` 命令。

### 本阶段范围与边界

- 范围：Python、本机单用户、默认仅绑定 `127.0.0.1`、带本机 Gateway Token、命令行控制、Telegram 迁移、SQLite 持久化和自动化测试。
- 暂不实现：官方控制 UI、插件市场、跨设备节点、多平台全部渠道、远程公开访问、OpenAI 兼容 API、容器沙箱和多租户服务。
- 安全底线：Gateway 未完成 Token 校验前，不允许处理聊天或工具请求；不得监听局域网或公网地址；真实密钥仍只存于 Git 忽略的本机配置。
- 迁移底线：不得静默删除或覆盖现有会话和长期记忆；迁移数据前先创建可恢复副本并用测试验证。

### 8.1 固化 Gateway 架构边界

**目标**：在写服务代码前，先明确组件职责和数据流，避免把“多几个启动脚本”误认为 Gateway。

**你将手写**：一份简短架构说明，写清 Gateway、CLI、渠道适配器、状态存储、Agent 运行器和工具策略的职责，以及它们之间唯一允许的调用方向。

**必须理解**：CLI 和渠道适配器不得直接创建或调用 Agent 运行器；它们只能向 Gateway 发请求。Gateway 是状态和副作用的唯一协调者。

**验证**：能画出“CLI / Telegram → Gateway → Agent、状态、工具、模型”的数据流，并用自己的话解释每条箭头。

**边界验证**：指出“Telegram 直接调用 Agent”为什么会破坏会话隔离、审计和任务管理。

**完成状态**：- [ ]

### 8.2 将项目整理为可安装包与 CLI 骨架

**目标**：提供不依赖文件路径的 `myclaw` 命令入口，为后续 CLI 和 Gateway 奠定稳定边界。

**你将手写**：Python 包配置、`myclaw --help`、`myclaw --version`、`myclaw config check` 的最小命令结构；首版优先使用标准库参数解析，避免过早增加 CLI 框架依赖。

**验证**：在独立虚拟环境安装项目后，可执行帮助、版本和配置检查命令。

**边界验证**：未知子命令必须失败并给出帮助提示；不得把配置中的密钥打印到终端。

**完成状态**：- [ ]

### 8.3 建立按会话隔离的 SQLite 状态层

**目标**：用事务化本地存储替代单一全局会话文件，为 Gateway 的多会话和可恢复任务做好准备。

**你将手写**：会话、消息、摘要和任务的最小数据模型；按 `session_id` 读取与写入的仓储接口；从旧本地记录导入时的显式备份和迁移流程。

**验证**：两个不同会话分别保存和恢复消息，重启后互不混淆。

**边界验证**：损坏记录、未知会话和重复迁移都必须安全处理，不能删除原始数据。

**完成状态**：- [ ]

### 8.4 启动仅本机可访问的 Gateway 服务

**目标**：建立常驻服务的最小生命周期，不立即暴露聊天能力。

**你将手写**：Gateway 启动、停止、健康检查和状态摘要；服务默认仅监听 `127.0.0.1`，并记录启动、停止和异常的脱敏日志。

**验证**：在一个终端前台运行 Gateway，在另一个终端通过 CLI 查询健康状态。

**边界验证**：错误端口、重复启动或非本机绑定请求必须被明确拒绝或安全处理。

**完成状态**：- [ ]

### 8.5 加入 Gateway Token 与会话消息 API

**目标**：让 Gateway 成为唯一处理消息的入口，并先建立认证边界。

**你将手写**：本机 Token 配置、认证中间层，以及发送消息和读取会话的最小 API；每个请求必须带 `session_id`。

**验证**：携带正确 Token 时，能向指定会话发送消息并获得 Agent 回复；同一会话在 Gateway 内恢复历史。

**边界验证**：缺少或错误 Token、空会话标识、过长请求和未知路径必须被拒绝，且不能调用模型或工具。

**完成状态**：- [ ]

### 8.6 让 CLI 成为 Gateway 客户端

**目标**：将日常操作统一为 CLI → Gateway，而不是 CLI 再次启动一个独立 Agent。

**你将手写**：`myclaw gateway run`、`myclaw gateway status`、`myclaw chat`、`myclaw sessions list` 和 `myclaw logs` 的最小命令集。

**验证**：Gateway 保持运行时，CLI 能发送消息、查询状态和列出会话；停止 Gateway 后，CLI 返回明确的不可用提示。

**边界验证**：CLI 不得读取或打印 Gateway Token；没有 Gateway 时不得偷偷在本地创建第二个 Agent。

**完成状态**：- [ ]

### 8.7 将 Telegram 改为 Gateway 渠道适配器

**目标**：Telegram 只负责接收、白名单校验和发送，不再拥有 Agent 或会话状态。

**你将手写**：Telegram → Gateway 请求适配、稳定的 Telegram 会话键映射和 Gateway 回复转发逻辑。

**验证**：终端 CLI 与 Telegram 对同一会话键的连续对话能看到同一段历史；不同会话键互不混淆。

**边界验证**：非白名单用户、群聊、非文本消息或 Gateway 不可用时，Telegram 不得绕过 Gateway 直接调用模型。

**完成状态**：- [ ]

### 8.8 将提醒升级为持久化 Gateway 任务

**目标**：让提醒由 Gateway 调度并在重启后恢复，不再依赖进程内计时器。

**你将手写**：持久化任务表、到期扫描或受控调度循环、任务状态机，以及 CLI 和 Telegram 的提醒创建入口。

**验证**：创建短时提醒后重启 Gateway，提醒仍只发送一次；任务结果可在状态查询中看到。

**边界验证**：重复启动 Gateway、重复执行到期扫描、超出资源上限或发送失败时，不得造成重复提醒或丢失任务状态。

**完成状态**：- [ ]

### 8.9 Gateway 安全、运维与最终验收

**目标**：为 Gateway MVP 固化可操作、可审计、可回归的完成标准。

**你将手写**：脱敏事件日志、Gateway 状态与诊断信息、代码级的工具策略最小实现、完整 README 更新和 Gateway 自动化测试。

**验证**：独立环境中完成“启动 Gateway → CLI 消息 → Telegram 消息 → 会话恢复 → 持久提醒 → 停止/重启 → 测试”的完整流程。

**边界验证**：错误 Token、非白名单消息、越权工具、损坏状态和服务不可用均有明确、无敏感信息的处理结果。

**完成状态**：- [ ]

## 4. 变更记录

| 日期 | 触发原因 | 改动 | 对进度/风险的影响 |
| --- | --- | --- | --- |
| 2026-03-05 | 初始计划 | 创建从零学习与实施路线 | 无 |
| 2026-03-05 | 已确认本机存在 Python 3.12.13，且学习者选择该版本 | 固定项目运行时为 Python 3.12 | 降低早期第三方库兼容性的不确定性 |
| 2026-03-05 | 学习者已具备 DeepSeek API Key；官方已退役 `deepseek-chat` | 使用 OpenAI 兼容接口与 `deepseek-v4-flash` | 避免使用已失效模型名称；首版优先控制成本与延迟 |
| 2026-03-05 | 学习者需要可复盘、可分享的实际指导记录 | 新增仅本机保存的 `实际指导过程复盘.md` 并纳入每步完成流程 | 保留真实验证和可复用经验，避免暴露过程与环境信息 |
| 2026-03-06 | 技能复盘的首次验证暴露出模型可能选择可用的写入工具 | 在技能工作流中规定：除非用户明确要求保存，否则只输出聊天内容；完成无写入复测 | 降低非预期副作用；该规则当前由技能文本约束，尚不是独立的代码级工具权限系统 |
| 2026-03-06 | 5.1 阶段只有终端文本渠道，无法实际接收图片或语音 | 将真实渠道的非文本消息验收移至 5.2；5.1 仅验证统一文本协议和空内容处理 | 避免把未实际发生的渠道行为当作已验证能力 |
| 2026-03-06 | 已确认可使用 Telegram | 按默认渠道接入 Telegram Bot API 长轮询与单用户白名单 | 真实消息渠道已可用；仍是单进程、单用户学习实现 |
| 2026-03-06 | 定时提醒无需模型决策，且不应污染聊天记忆 | 6.1 改为受限 `/remind` 命令触发的独立任务执行器 | 降低副作用和上下文串扰；未触发任务不跨进程保存 |
| 2026-03-06 | 单用户顺序长轮询不存在并发 Agent 回合 | 6.2 标记为当前范围不适用，转入 6.3 自动化回归测试 | 避免为不存在的并发路径引入线程和锁；多用户或并行化时需重新设计 |
| 2026-03-07 | 关键能力已完成手工验证，需要防止后续改动造成安全回归 | 新增 pytest 自动化回归测试，覆盖配置、受限工具、会话记录、Telegram 白名单和提醒命令 | 12 项测试可重复通过；白名单判断被临时改坏时，相关测试会失败 |
| 2026-03-07 | 项目需要能被未来使用者重新安装、理解边界并重复验证 | 补充固定版本依赖清单、公开使用说明和最终验收；使用独立 Python 3.12 虚拟环境验证安装与测试 | 当前学习范围完成；外部模型与 Telegram 验证仍依赖使用者自己的私密凭据 |
| 2026-03-07 | 学习者希望采用 Gateway 与 CLI 形态继续扩展 | 新增第二阶段 8.1–8.9：从架构边界、CLI、SQLite 状态层到本机 Gateway、渠道迁移和持久任务 | 继续保持单用户、本机优先与逐步迁移，不以官方全量复刻为目标 |

## 5. 参考资料的使用方式

- `czl9707/build-your-own-openclaw`：作为渐进功能清单和实现思路的主要参考；
- `dabit3` 的文章：用于理解会话、记忆、工具、队列和定时任务为何存在；
- `openclaw/openclaw`：作为架构与安全方向的参考，不作为需复刻的范围。
